// Matches the two types GovernmentOpportunityCard.tsx knows how to
// accent-color -- anything else falls back to its default color there,
// so keep this list in sync with that component if a third type is
// ever needed.
export const listingTypes = ["Job Recruitment", "Competitive Exam"] as const;
